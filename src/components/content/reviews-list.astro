---
import Link from "@components/link.astro";
import type { IReview } from "@utils/ts/aniListApiTypes";

export interface Props {
  reviews: IReview[];
}

const { reviews } = Astro.props;
---

<div class="mb-10 columns-1 items-start gap-4 md:columns-2 lg:columns-3">
  {
    reviews.length &&
      reviews.map((review) => (
        <div class="border-lightBlue mb-4 flex w-full break-inside-avoid flex-col border bg-gray-800 p-7 shadow-md md:gap-4">
          <p class="line-clamp-3 font-manrope mb-2 text-center text-xl font-semibold">
            <span class="text-lightPurple mr-0.5 text-2xl">🙶</span>
            {review.node.summary}
            <span class="text-lightPurple ml-0.5 text-2xl">🙷</span>
          </p>
          <div class="flex justify-end">
            <Link
              text="review"
              isExternalHref={true}
              href={review.node.siteUrl}
            >
              <span class="text-sm opacity-70">
                ver <i>review</i> na íntegra
              </span>
            </Link>
          </div>
        </div>
      ))
  }
</div>
